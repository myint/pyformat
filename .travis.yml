language: python

python:
    - "2.7"
    - "3.2"
    - "3.3"

install:
    - python setup.py --quiet install

script:
    - python test_pyformat.py
    - coverage run test_pyformat.py; true
    - coverage report --show-missing --omit='*/distutils/*'; true
